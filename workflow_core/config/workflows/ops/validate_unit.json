{
    "name": "Ops.Validate.Unit",
    "description": "Run Unit Tests",
    "steps": [
        {
            "id": "unit_test",
            "type": "atom",
            "ref": "Run_Command",
            "args": {
                "command": "${unit_test_cmd}",
                "description": "Execute Unit Tests"
            }
        }
    ]
}